# Git 工作流程文档

本文档介绍如何在 `graph-toucan` 项目中使用 Git 进行版本控制和代码管理。

---

## 📦 仓库信息

- **仓库名称**: graph-toucan
- **远程地址**: https://github.com/h66840/graph-toucan.git
- **主分支**: main
- **克隆方式**: HTTPS

---

## 🚀 快速开始

### 1. 克隆仓库（首次使用）

```bash
git clone https://github.com/h66840/graph-toucan.git
cd graph-toucan
```

### 2. 查看当前状态

```bash
git status
```

### 3. 基本工作流程

```bash
# 1. 修改文件后，查看更改
git status
git diff

# 2. 添加文件到暂存区
git add .                    # 添加所有更改
git add <文件名>              # 添加特定文件

# 3. 提交更改
git commit -m "描述你的更改"

# 4. 推送到远程仓库
git push
```

---

## 🎯 理解 Git 的三个区域

Git 有三个重要区域，理解它们的关系是掌握 Git 的关键：

```
┌─────────────────┐
│  Working Dir    │ ← 你实际看到的文件（工作区）
│   (工作区)       │
└────────┬────────┘
         │ git add
         ↓
┌─────────────────┐
│  Staging Area   │ ← 暂存区/索引区（准备 commit 的内容）
│   (暂存区)       │
└────────┬────────┘
         │ git commit
         ↓
┌─────────────────┐
│     HEAD        │ ← 最后一次提交（仓库中的快照）
│   (当前提交)     │
└─────────────────┘
```

### 三个区域的含义

1. **Working Directory (工作区)**
   - 你实际看到的文件
   - 修改后的文件会在这里显示为 `modified`

2. **Staging Area (暂存区/索引区)**
   - 也叫 `Index`
   - 执行 `git add` 后，文件被添加到这里
   - 这里的文件会被下一次 `git commit` 提交

3. **HEAD (当前提交)**
   - 仓库中最后一次提交的快照
   - 执行 `git commit` 后，暂存区的内容会变成新的 HEAD

### git reset 的原理

```bash
git reset
```

**执行过程**：
1. 将暂存区重置为 HEAD 的状态（撤销 `git add`）
2. 不改变工作区的文件
3. Git 比较工作区和新的暂存区，发现差异 → "Unstaged changes"

**图解**：
```
之前（git add 后）：
  Working Dir  ===  Staging Area  ≠  HEAD
                        ↑
                   准备提交这些

执行 git reset 后：
  Working Dir  ≠  Staging Area === HEAD
                     ↑
              重置回 HEAD 状态
              但工作区没变
              → Unstaged changes!
```

### 查看暂存区的详细命令

```bash
# 1. 查看整体状态（最常用）
git status

# 2. 查看暂存区与 HEAD 的差异
git diff --cached
# 或（等价命令）
git diff --staged

# 3. 查看暂存区的文件列表
git ls-files -s        # 查看详细信息
git ls-files           # 只看文件名

# 4. 查看暂存区中特定文件的内容
git show :README.md

# 5. 查看暂存区改动的统计
git diff --cached --stat
```

### 常见场景示例

**场景 1：修改文件后查看状态**
```bash
# 修改 README.md
git status
# 输出：Changes not staged for commit: README.md

git diff README.md
# 显示工作区与暂存区的差异

# 如果暂存区是空的，这会显示工作区与 HEAD 的差异
```

**场景 2：添加文件到暂存区**
```bash
git add README.md
git status
# 输出：Changes to be committed: README.md

git diff --cached
# 显示暂存区与 HEAD 的差异（将提交的内容）
```

**场景 3：撤销暂存**
```bash
git reset HEAD README.md
# 或（简单写法）
git reset README.md

git status
# 输出：Changes not staged for commit: README.md
# 文件回到工作区，但保留修改
```

**场景 4：完全丢弃工作区的修改**
```bash
# 方法 1：从暂存区恢复（如果暂存区有该文件）
git checkout -- README.md

# 方法 2：从 HEAD 恢复（新命令）
git restore README.md

# 方法 3：强制重置工作区为 HEAD（危险！会丢失所有未提交的修改）
git reset --hard HEAD
```

---

## 📋 常用 Git 命令

### 查看状态和历史

```bash
# 查看当前状态
git status

# 查看提交历史
git log
git log --oneline           # 简洁模式
git log --graph --oneline   # 图形化显示

# 查看具体更改
git diff                    # 查看工作区的更改
git diff --staged           # 查看暂存区的更改
git diff HEAD~1             # 与上一次提交对比
```

### 添加和提交

```bash
# 添加文件
git add <文件名>            # 添加特定文件
git add .                   # 添加所有更改
git add *.py                # 添加所有 Python 文件

# 提交
git commit -m "提交信息"
git commit -am "提交信息"   # 添加并提交已跟踪的文件
git commit --amend          # 修改上一次提交
```

### 分支管理

```bash
# 查看分支
git branch                  # 查看本地分支
git branch -a               # 查看所有分支（包括远程）

# 创建和切换分支
git branch <分支名>         # 创建新分支
git checkout <分支名>       # 切换分支
git checkout -b <分支名>    # 创建并切换到新分支
git switch <分支名>         # 切换分支（新命令）

# 合并分支
git merge <分支名>          # 合并指定分支到当前分支

# 删除分支
git branch -d <分支名>      # 删除本地分支
git push origin --delete <分支名>  # 删除远程分支
```

### 远程仓库操作

```bash
# 查看远程仓库
git remote -v

# 拉取更新
git pull                    # 拉取并合并
git fetch                   # 仅拉取，不合并

# 推送更改
git push                    # 推送到默认远程分支
git push origin main        # 推送到指定分支
git push -u origin <分支名>  # 首次推送新分支
```

### 撤销和回退

```bash
# 撤销工作区的更改
git checkout -- <文件名>
git restore <文件名>        # 新命令

# 取消暂存
git reset HEAD <文件名>
git restore --staged <文件名>  # 新命令

# 回退提交
git reset --soft HEAD~1     # 撤销提交，保留更改在暂存区
git reset --mixed HEAD~1    # 撤销提交，保留更改在工作区
git reset --hard HEAD~1     # 撤销提交，丢弃所有更改（危险！）

# 查看历史版本
git reflog                  # 查看所有操作记录
git reset --hard <commit-id>  # 回退到指定提交
```

---

## 🔄 推荐工作流程

### 日常开发流程

```bash
# 1. 开始工作前，先拉取最新代码
git pull

# 2. 创建功能分支（可选，推荐）
git checkout -b feature/新功能名称

# 3. 进行开发工作
# ... 编写代码 ...

# 4. 查看更改
git status
git diff

# 5. 添加并提交更改
git add .
git commit -m "feat: 添加新功能描述"

# 6. 推送到远程
git push

# 7. 如果是功能分支，合并到主分支
git checkout main
git merge feature/新功能名称
git push
```

### 提交信息规范

建议使用语义化提交信息：

```bash
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整（不影响功能）
refactor: 代码重构
test: 测试相关
chore: 构建过程或辅助工具的变动
```

**示例**：
```bash
git commit -m "feat: 实现基于图的工具依赖关系构建算法"
git commit -m "fix: 修复 miss-params 数据生成中的参数缺失问题"
git commit -m "docs: 更新论文大纲和实验设计文档"
```

---

## 🛠️ 高级操作

### 暂存当前工作

```bash
# 暂存当前更改
git stash
git stash save "暂存描述"

# 查看暂存列表
git stash list

# 恢复暂存
git stash pop               # 恢复并删除暂存
git stash apply             # 恢复但保留暂存

# 删除暂存
git stash drop
git stash clear             # 清空所有暂存
```

### 标签管理

```bash
# 创建标签
git tag v1.0.0
git tag -a v1.0.0 -m "版本 1.0.0"

# 查看标签
git tag
git show v1.0.0

# 推送标签
git push origin v1.0.0
git push origin --tags      # 推送所有标签

# 删除标签
git tag -d v1.0.0           # 删除本地标签
git push origin --delete v1.0.0  # 删除远程标签
```

### Cherry-pick（挑选提交）

```bash
# 将其他分支的某个提交应用到当前分支
git cherry-pick <commit-id>
```

---

## 🔐 SSH 密钥配置（可选）

如果想使用 SSH 方式推送（`git@github.com:h66840/graph-toucan.git`），需要配置 SSH 密钥：

### 1. 生成 SSH 密钥

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# 或者使用 RSA
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

### 2. 添加到 SSH Agent

```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

### 3. 复制公钥

```bash
cat ~/.ssh/id_ed25519.pub
# 或者
pbcopy < ~/.ssh/id_ed25519.pub  # macOS 直接复制到剪贴板
```

### 4. 添加到 GitHub

1. 访问 https://github.com/settings/keys
2. 点击 "New SSH key"
3. 粘贴公钥内容
4. 保存

### 5. 切换到 SSH 方式

```bash
git remote set-url origin git@github.com:h66840/graph-toucan.git
```

---

## 📝 .gitignore 配置

项目中的 `.gitignore` 文件用于忽略不需要版本控制的文件：

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# 项目特定
*.log
.DS_Store
.env
*.db
```

---

## ⚠️ 注意事项

### 1. 敏感信息保护

**永远不要提交以下内容**：
- API 密钥和令牌
- 密码和凭证
- 私钥文件
- 个人身份信息

如果不小心提交了敏感信息：
```bash
# 从历史中完全删除（危险操作！）
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch <文件路径>" \
  --prune-empty --tag-name-filter cat -- --all

# 强制推送
git push origin --force --all
```

### 2. 避免强制推送

```bash
# 危险操作！会覆盖远程历史
git push --force

# 更安全的方式
git push --force-with-lease
```

### 3. 定期拉取更新

在开始工作前，始终先拉取最新代码：
```bash
git pull
```

### 4. 提交前检查

```bash
# 查看将要提交的内容
git diff --staged

# 确认无误后再提交
git commit -m "提交信息"
```

---

## 🆘 常见问题解决

### 1. 合并冲突

```bash
# 发生冲突时
git status                  # 查看冲突文件

# 手动编辑冲突文件，解决冲突标记：
# <<<<<<< HEAD
# 你的更改
# =======
# 其他人的更改
# >>>>>>> branch-name

# 解决后
git add <冲突文件>
git commit -m "解决合并冲突"
```

### 2. 误提交了文件

```bash
# 从暂存区移除
git reset HEAD <文件名>

# 从最后一次提交中移除
git reset --soft HEAD~1
git reset HEAD <文件名>
git commit -m "正确的提交"
```

### 3. 忘记切换分支就开始工作

```bash
# 暂存当前工作
git stash

# 切换到正确的分支
git checkout <正确的分支>

# 恢复工作
git stash pop
```

### 4. 推送被拒绝

```bash
# 远程有新提交时
git pull --rebase          # 拉取并变基
git push                   # 再次推送
```

---

## 📚 学习资源

- [Git 官方文档](https://git-scm.com/doc)
- [GitHub 官方指南](https://docs.github.com/)
- [Pro Git 书籍（中文版）](https://git-scm.com/book/zh/v2)
- [Git 可视化学习](https://learngitbranching.js.org/)

---

## 📞 项目相关

- **项目说明**: 基于工具依赖图的高质量多轮对话工具调用数据集生成系统
- **主要文件**:
  - `paper.md` - 论文草稿
  - `论文大纲.md` - 论文结构大纲
  - `main.py` - 主程序
  - `README.md` - 项目说明

---

**最后更新**: 2026-01-21
